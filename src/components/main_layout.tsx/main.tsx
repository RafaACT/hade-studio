import React from "react";

export const Main = ({children}:any) => {

    return(
        <div>
            <nav>
                hello
            </nav>
            <div>
                {children}
            </div>
            <footer>
                hade studio
            </footer>
        </div>
    )
}